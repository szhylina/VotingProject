<!DOCTYPE HTML>
<html>
<head>
    <title>VotingApp</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
</head>
<body>
	<div style="width: 100%; height: 55px;font-size: 15pt;background-color: #339999; text-align: center">
    	<a style="color: white; margin:5%" href="/result">RESULTS</a>
    	<a style="color: white; margin:5%" href="/">HOME</a>
    	<form action="/logout" method="post">
    		<input type="submit" value="Sign Out"/>
    		<input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
    	</form>
	</div>
    <div style="width: 100%; height: 55px;font-size: 15pt; text-align: center">
		<form action="/voting" method="post">
			 {{#votings}}
			 	<div><b>Choose one candidate for Voting "{{title}}"</b></div>
			 	<input type="hidden" name="voting" value="{{id}}"/>
			 {{/votings}}
			 {{#candidates}}
				 <div>
				 	<u><input type="radio" name="candidate" value="{{id}}" style="margin-top: 10px" />{{name}}</u>
				 </div>
			 {{/candidates}}
			 <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
			 <button type="submit" style="font-size: 15pt">Vote</button>
		</form>
	</div>
</body>
</html>